services:
  bot:
    build: .
    container_name: egov-debt-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "python", "healthcheck.py"]
      interval: 15m
      timeout: 90s
      retries: 2
      start_period: 2m
